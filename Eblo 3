Вот пример программы на языке Java, которая создает заданное пользователем количество узлов и выводит их в консоль по порядку:

import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        Scanner scanner = new Scanner(System.in);
        System.out.print("Введите количество узлов: ");
        int n = scanner.nextInt();

        for (int i = 1; i <= n; i++) {
            System.out.println("Узел " + i);
        }
    }
}

Сначала программа запрашивает у пользователя количество узлов, которые нужно создать. Затем она использует цикл for для создания каждого узла и вывода его номера в консоль. Каждый узел имеет номер от 1 до заданного пользователем числа n.
